version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn install
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
artifacts:
  files:
    -/home/rk95/Downloads/Order/target/Order.jar